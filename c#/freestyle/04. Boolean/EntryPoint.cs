class EntryPoint
{
    static void Main()
    {
        bool notAlive = false; // it alive
        bool isAlive = true; // its not alive

        bool isEqual = (0 == 0);

        System.Console.WriteLine(notAlive);
        System.Console.WriteLine(isAlive);
        System.Console.WriteLine(isEqual);
    }
}